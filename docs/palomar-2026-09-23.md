# Palomar submission, 2026-09-23

Rung 2 was submitted to Palomar on 2026-09-23 and returned **changes required**. The proof was not
at fault: Palomar now requires a newer Lean toolchain than this repository pins.

- **Commit:** `7118d68eabffe49e41a672d16eb8ddba2608f03d`
- **Comparator configuration:** `comparator.json`. The target is `Erdos1007Dim5.Palomar.target`, and
  the permitted axioms are `propext`, `Quot.sound` and `Classical.choice`.
- **Authorization basis:** a responsible author or maintainer. The owner confirmed the repository, the
  commit, the configuration and that relationship before submitting.

## Before submitting: the preflight

The `Palomar preflight` workflow ran Palomar's verification pipeline at `f49b4f2` on this commit. Its
mechanical report reads `status: pass`, with no errors or warnings: [run 35914081642][preflight].
An earlier run on `1b689c8` also passed.

## Palomar's verification

Palomar's own run, [35915133104][verify], used its pipeline as of 2026-09-23 and failed in the
preparation phase, before any Lean was checked:

> `toolchain.unsupported`: Lean toolchain `leanprover/lean4:v4.33.1` is older than the minimum
> Palomar supports (`v4.35.0-rc2`).

Palomar marks this as not retryable, and its next action is to update the repository, commit, and
submit again with the new commit. The preflight did not catch it because the preflight workflow pins
the pipeline at `f49b4f2`, from before this minimum existed; Palomar's current pipeline is `1703d7b`.
Erdos1007 was registered on 2026-09-22, before the minimum, on the same `v4.33.1`.

## What it needs

This repository and its `GraphDimension` dependency must move to Lean `v4.35.0-rc2` or later, with a
matching Mathlib. The preflight workflow must pin a current pipeline commit. Then the gates, the
probes and Comparator run again, and a new submission is made at the new commit.
`formal-conjectures` still pins `v4.33.1`, so its link can stay on a `v4.33.1` commit.

## How it was submitted

The workspace's agent used Palomar's agent path. It proved push access with a temporary tag on this
repository and a secret gist, and deleted both once Palomar accepted the proof. Nothing was
registered.

[preflight]: https://github.com/Dishah3241/Erdos1007Dim5/actions/runs/35914081642
[verify]: https://github.com/PalomarRegistry/PalomarSubmission/actions/runs/35915133104

## The port

Commit `67ee342` moves this repository to Lean and Mathlib `v4.35.0-rc2`, and pins `GraphDimension` at
`40d6dbca09967a47fe0c06034cf0a7c70c93a003`, the same library ported. The preflight now pins Palomar's
current pipeline, `1703d7b`. No proof here changed. `lint-env` gained `tacticAlt`, Batteries' new default
linter, which finds nothing. On the ported tree every gate passes, with the audit probes 10/10, and
`checkdecls` passes. `lake comparator` accepts under both kernels (`docs/comparator-2026-09-23.md`).

## Resubmission

The owner confirmed the ported commit, and it was submitted on 2026-09-23: `85032a43ca6db7d92b492efd05deeea0739c4570`,
with the same configuration and authorization basis. The preflight against Palomar's current pipeline
passed first, [run 35928072331][preflight2], with `status: pass` and no errors or warnings. Access was
proved the same way, and both proof artifacts were deleted. Verification and review are in progress;
nothing is registered.

[preflight2]: https://github.com/Dishah3241/Erdos1007Dim5/actions/runs/35928072331
